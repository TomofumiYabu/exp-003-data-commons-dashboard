import fs from 'node:fs';import path from 'node:path';
const patterns=[/AIza[0-9A-Za-z_-]{35}/,/gh[pousr]_[0-9A-Za-z]{20,}/,/github_pat_[0-9A-Za-z_]{20,}/,/-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----/,/(?:api[_-]?key|access[_-]?token|password|secret)\s*[:=]\s*["']?[A-Za-z0-9_/-]{16,}/i];
let count=0;const suspicious=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(['.git','node_modules','.codex'].includes(e.name))continue;const p=path.join(dir,e.name);if(e.isDirectory()){walk(p);continue;}if(/\.(png|jpg|jpeg|webp)$/i.test(p))continue;count++;const content=fs.readFileSync(p,'utf8');if(patterns.some(r=>r.test(content))||/^\.env(?:\.|$)/.test(e.name))suspicious.push(p);}}walk('.');console.log(JSON.stringify({scannedTextFiles:count,suspectedSecretFiles:suspicious,limitations:'Heuristic scan, not a guarantee. Matches are never printed.'}));if(suspicious.length)process.exit(2);
