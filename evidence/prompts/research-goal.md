# Applied research goal

Following the user-supplied EXP-003 v1.1 plan: compare Japan, Kenya and Mexico for total population, life expectancy at birth and under-five mortality over 2015–2024. Resolve variables, inspect definitions/units/sources, retrieve real observations, preserve missing values and trace findings to machine-readable evidence. Continue autonomously except at explicit STOP conditions.

Arm A used Data Commons MCP search → metadata → bounded observations. Two official World Bank indicator pages were additionally opened to corroborate health indicator labels/sources; this is a protocol deviation from a strictly MCP-only arm, not counted as MCP tool steps.

Arm B applied the same research goal using web search/page text only, with no MCP calls during that phase. It reused this conversation's context and did not replay an identical isolated prompt; it is an exploratory baseline rather than a clean controlled trial.
